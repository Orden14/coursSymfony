{% extends 'base.html.twig' %}

{% block title %}Créer une annonces{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1>{{ action }} une annonce</h1>

        {{ form_errors(annonceForm) }}

        {{ form_start(annonceForm, {'attr': {'class': 'form-group'}}) }}
            <div class="form-group">
                {{ form_row(annonceForm.title, {'attr': {'class': 'form-control'}}) }}
            </div>
            <div class="form-group">
                {{ form_row(annonceForm.description, {'attr': {'class': 'form-control'}}) }}
            </div>
            <div class="form-group">
                {{ form_row(annonceForm.price, {'attr': {'class': 'form-control'}})}}
            </div>
            <div class="form-group">
                {{ form_row(annonceForm.image, {'attr': {'class': 'form-control'}})}}
            </div>

            <button type="submit" class="btn btn-primary cmargintop">Publier</button>
        {{ form_end(annonceForm) }}
    </div>   
{% endblock %}
